import { Container } from "@mui/material";
import React, { useEffect } from "react";

const Like = () => {
  useEffect(() => {
    window.scrollTo(0, 0);
  }, []);

  return <Container>Like</Container>;
};

export default Like;
